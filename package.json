{
  "name": "parts-mart-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Parts Mart REST API — zero external dependencies",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
